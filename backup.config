# backup.config - edit to fit your environment


# Destination directory for backup archives
BACKUP_DESTINATION="$HOME/backup-bash-project/Backups"




# Comma-separated exclude patterns (tar --exclude=pattern)
EXCLUDE_PATTERNS=".git,node_modules,.cache,*.log,temp"


# Rotation policy
DAILY_KEEP=7 # keep last 7 daily backups
WEEKLY_KEEP=4 # keep 4 weekly backups
MONTHLY_KEEP=3 # keep 3 monthly backups


# Minimum required free space in MB (script will abort if less)
MIN_SPACE_MB=100


# Dry run default (0 = false, 1 = true)
DEFAULT_DRY_RUN=0


# Email recipient (script writes to email.txt; not sending real mail)
EMAIL_RECIPIENT="admin@example.com"


# Snapshot file path for incremental backups
SNAPSHOT_FILE="$PWD/backup.snar"


# Prefix for backup file names
BACKUP_PREFIX="backup"


# Date format used in filenames
DATE_FORMAT="%Y-%m-%d-%H%M"


# Compression (tar flags, you can modify)
TAR_OPTS="-czf"


# Lockfile path
LOCK_FILE="/tmp/backup.lock"


# Log file
LOG_FILE="$PWD/backup.log"
